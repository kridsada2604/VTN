import Link from "next/link";import {createClient}from"@/lib/supabase/server";import{getCurrentCompanyId}from"@/lib/current-company";import{PageHeader}from"@/components/page-header";import{FormCard}from"@/components/master-data/form-card";import{StatusBadge}from"@/components/master-data/status-badge";import{saveCategory,toggleCategory}from"./actions";
export default async function Page({searchParams}:{searchParams:Promise<{edit?:string}>}){const{edit=""}=await searchParams,s=await createClient(),companyId=await getCurrentCompanyId();const{data:rows=[]}=await s.from("categorys").select("*").eq("company_id",companyId).order("code");const e=edit?rows?.find(x=>x.id===edit):undefined;return <div><PageHeader eyebrow="INVENTORY" title="หมวดหมู่สินค้า" description="จัดกลุ่มสินค้าเพื่อการค้นหาและรายงาน"/><div className="two-column-page mt-6"><section className="card table-wrap"><table className="data-table"><thead><tr><th>รหัส</th><th>ชื่อหมวดหมู่</th><th>สถานะ</th><th>จัดการ</th></tr></thead><tbody>{rows?.map(x=><tr key={x.id}><td className="font-bold">{x.code}</td><td>{x.name}</td><td><StatusBadge active={x.is_active}/></td><td><div className="action-row"><Link className="btn-secondary btn-small" href={`/inventory/categorys?edit=${x.id}`}>แก้ไข</Link><form action={toggleCategory}><input type="hidden" name="id" value={x.id}/><input type="hidden" name="next" value={String(!x.is_active)}/><button className="btn-secondary btn-small">{x.is_active?"ปิด":"เปิด"}</button></form></div></td></tr>)}</tbody></table></section><FormCard title={e?"แก้ไขหมวดหมู่สินค้า":"เพิ่มหมวดหมู่สินค้า"}><form action={saveCategory} className="form-grid"><input type="hidden" name="id" value={e?.id||""}/><label><span className="label">รหัส *</span><input className="input" name="code" required defaultValue={e?.code}/></label><label><span className="label">ชื่อหมวดหมู่ *</span><input className="input" name="name" required defaultValue={e?.name}/></label><div className="full action-row"><button className="btn-primary">บันทึก</button>{e&&<Link className="btn-secondary" href="/inventory/categorys">ยกเลิก</Link>}</div></form></FormCard></div></div>}
