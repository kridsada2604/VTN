import { createClient } from "@/lib/supabase/server";
export default async function CompanyPage(){const supabase=await createClient();const {data}=await supabase.from('companies').select('code,name_th,name_en,tax_id').limit(1).maybeSingle();return <div><h1 className="text-3xl font-black">ข้อมูลบริษัท</h1><div className="card mt-6 max-w-3xl p-6"><dl className="grid gap-5 sm:grid-cols-2"><Item k="รหัสบริษัท" v={data?.code}/><Item k="ชื่อภาษาไทย" v={data?.name_th}/><Item k="ชื่อภาษาอังกฤษ" v={data?.name_en}/><Item k="เลขประจำตัวผู้เสียภาษี" v={data?.tax_id}/></dl><p className="mt-6 text-sm text-gray-500">แก้ไขข้อมูลผ่าน Supabase Table Editor ใน Phase 1</p></div></div>}
function Item({k,v}:{k:string;v?:string|null}){return <div><dt className="text-sm text-gray-500">{k}</dt><dd className="mt-1 font-bold">{v||'-'}</dd></div>}
