import Link from "next/link"; import {PageHeader} from "@/components/page-header"; import {QuotationForm} from "@/components/sales/quotation-form"; import {createClient} from "@/lib/supabase/server"; import {getCurrentCompanyId} from "@/lib/current-company"; import {saveQuotation} from "../actions";
export default async function Page(){const s=await createClient(),companyId=await getCurrentCompanyId(); const [{data:customers=[]},{data:products=[]}]=await Promise.all([s.from("customers").select("id,code,name").eq("company_id",companyId).eq("is_active",true).order("name"),s.from("products").select("id,sku,name,selling_price").eq("company_id",companyId).eq("is_active",true).order("name")]); return <div><PageHeader eyebrow="SALES" title="สร้างใบเสนอราคา" description="เลือกลูกค้าและเพิ่มรายการสินค้า"/><div className="mt-6 mb-4"><Link className="btn-secondary" href="/sales/quotations">← กลับรายการ</Link></div><QuotationForm customers={customers||[]} products={(products||[]).map(x=>({...x,selling_price:Number(x.selling_price)}))} action={saveQuotation}/></div>}
