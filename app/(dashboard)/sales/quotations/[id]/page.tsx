import Link from "next/link"; import {notFound} from "next/navigation"; import {PageHeader} from "@/components/page-header"; import {createClient} from "@/lib/supabase/server"; import {getCurrentCompanyId} from "@/lib/current-company"; import {updateQuotationStatus} from "../actions";
type QuotationItem={id:string;description:string;quantity:number|string;unit_price:number|string;line_discount:number|string;line_tax:number|string;line_total:number|string};
export default async function Page({params}:{params:Promise<{id:string}>}){const {id}=await params,s=await createClient(),companyId=await getCurrentCompanyId(); const {data:q}=await s.from("sales_quotations").select("*,customers(*)").eq("id",id).eq("company_id",companyId).maybeSingle(); if(!q) notFound(); const {data:items=[]}=await s.from("sales_quotation_items").select("*").eq("quotation_id",id).order("sort_order"); return <div><PageHeader eyebrow="QUOTATION" title={q.document_no} description={`${q.customers?.name||""} • ${q.quotation_date}`}/><div className="action-row my-6"><Link className="btn-secondary" href="/sales/quotations">← กลับรายการ</Link>{["DRAFT","SENT","ACCEPTED","REJECTED","CANCELLED"].map(st=><form action={updateQuotationStatus} key={st}><input type="hidden" name="id" value={id}/><input type="hidden" name="status" value={st}/><button className={q.status===st?"btn-primary":"btn-secondary"}>{st}</button></form>)}</div><section className="card p-5"><div className="grid gap-4 sm:grid-cols-3"><div><p className="text-sm text-gray-500">ลูกค้า</p><p className="font-bold">{q.customers?.name}</p></div><div><p className="text-sm text-gray-500">ใช้ได้ถึง</p><p className="font-bold">{q.valid_until||"-"}</p></div><div><p className="text-sm text-gray-500">สถานะ</p><p className="font-bold">{q.status}</p></div></div><div className="table-wrap mt-6"><table className="data-table"><thead><tr><th>รายละเอียด</th><th>จำนวน</th><th>ราคา</th><th>ส่วนลด</th><th>ภาษี</th><th>รวม</th></tr></thead><tbody>{(items as QuotationItem[] | null)?.map((x)=><tr key={x.id}><td>{x.description}</td><td>{x.quantity}</td><td>{Number(x.unit_price).toLocaleString()}</td><td>{Number(x.line_discount).toLocaleString()}</td><td>{Number(x.line_tax).toLocaleString()}</td><td className="font-bold">{Number(x.line_total).toLocaleString()}</td></tr>)}</tbody></table></div><div className="ml-auto mt-6 max-w-sm space-y-2 text-right"><p>ยอดก่อนลด: ฿{Number(q.subtotal).toLocaleString("th-TH",{minimumFractionDigits:2})}</p><p>ส่วนลด: ฿{Number(q.discount_amount).toLocaleString("th-TH",{minimumFractionDigits:2})}</p><p>ภาษี: ฿{Number(q.tax_amount).toLocaleString("th-TH",{minimumFractionDigits:2})}</p><p className="text-2xl font-black">รวม ฿{Number(q.total_amount).toLocaleString("th-TH",{minimumFractionDigits:2})}</p></div>{q.notes&&<p className="mt-6 rounded-xl bg-gray-50 p-4">{q.notes}</p>}</section></div>}
